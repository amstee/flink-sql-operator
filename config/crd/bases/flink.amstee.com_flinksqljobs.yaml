---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: flinksqljobs.flink.amstee.com
spec:
  group: flink.amstee.com
  names:
    kind: FlinkSqlJob
    listKind: FlinkSqlJobList
    plural: flinksqljobs
    singular: flinksqljob
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.sessionHandle
      name: SessionHandle
      type: string
    - jsonPath: .status.startTime
      name: StartTime
      type: date
    - jsonPath: .status.completionTime
      name: CompletionTime
      type: date
    - jsonPath: .status.globalStatus
      name: Status
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FlinkSqlJob is the Schema for the flinksqljobs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FlinkSqlJobSpec defines the desired state of FlinkSqlJob
            properties:
              configurations:
                description: Configurations will be passed to the Flink SQL Gateway
                  to configure the session for this jobs.
                items:
                  type: string
                type: array
              statement:
                description: SQL statement of the job.
                type: string
            required:
            - statement
            type: object
          status:
            description: FlinkSqlJobStatus defines the observed state of FlinkSqlJob
            properties:
              completionTime:
                description: Represents time when the job was completed.
                format: date-time
                type: string
              conditions:
                items:
                  description: StatusCondition describes the state a job or operation
                    went through at a certain point.
                  properties:
                    lastProbeTime:
                      description: Last time the condition was checked.
                      format: date-time
                      type: string
                    lastTransitionTime:
                      description: Last time the condition transit from one status
                        to another.
                      format: date-time
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: (brief) reason for the condition's last transition.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: Type of job condition, Complete, Running or Failed.
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              globalStatus:
                description: Represents the global status of the job.
                type: string
              operations:
                description: Operations of the job and their statuses.
                items:
                  description: OperationStatus represents the status of an operation.
                  properties:
                    conditions:
                      items:
                        description: StatusCondition describes the state a job or
                          operation went through at a certain point.
                        properties:
                          lastProbeTime:
                            description: Last time the condition was checked.
                            format: date-time
                            type: string
                          lastTransitionTime:
                            description: Last time the condition transit from one
                              status to another.
                            format: date-time
                            type: string
                          message:
                            description: Human-readable message indicating details
                              about last transition.
                            type: string
                          reason:
                            description: (brief) reason for the condition's last transition.
                            type: string
                          status:
                            description: Status of the condition, one of True, False,
                              Unknown.
                            type: string
                          type:
                            description: Type of job condition, Complete, Running
                              or Failed.
                            type: string
                        required:
                        - status
                        - type
                        type: object
                      type: array
                    operationHandle:
                      description: The operation handle.
                      type: string
                    operationID:
                      description: Represents the operation ID.
                      type: integer
                  required:
                  - operationHandle
                  - operationID
                  type: object
                type: array
              sessionHandle:
                description: Session handle of the job.
                type: string
              startTime:
                description: Represents time when the job controller started processing
                  a job.
                format: date-time
                type: string
            required:
            - globalStatus
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
